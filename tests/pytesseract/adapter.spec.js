const {Query} = require("../../dist/index.cjs");

describe("pytesseract/adapter", () => {
  describe("buildSearchParams", () => {
    it("should build a plain TesseractDataRequest object", () => {
      const query = new Query();
    });
  });
});
